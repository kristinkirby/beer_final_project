 <% if @current_user == nil %>
    <% photo = "https://freesvg.org/img/abstract-user-flat-4.png"%>
  <% else %>
    <% photo = @current_user.photo %>
  <% end %>



<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">Home</a>
    <a class="navbar-brand" href="/beers">Beers</a> 
    <a class="navbar-brand" href="/breweries">Breweries</a>
    

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsible-nav-links" aria-controls="collapsible-nav-links" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="collapsible-nav-links">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <img class = "navbar-brand img-fluid rounded-circle user_img_sm" 
                      src = <%=photo%>>
          </a>
          <% if @current_user == nil %>
          
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a href="/user_sign_in" class="dropdown-item">Sign in</a>
              <a href="/user_sign_up" class="dropdown-item">Sign up</a>
            <div>
          <% else %>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <a href="/users/<%=@current_user.id%>" class="dropdown-item">My Profile</a>
              <a href="/users/<%=@current_user.id%>" class="dropdown-item">Edit Profile</a>
              <a href="/users/<%=@current_user.id%>" class="dropdown-item">Favorites</a>
              <a href="/users/<%=@current_user.id%>" class="dropdown-item">Flagged</a>
              <hr class = "dropdown-divider">
              <a href="/user_sign_out" class="dropdown-item">Sign out</a>
            </div>
          <% end %>
        </li>
      </ul>
    </div> <!-- .navbar-collapse -->
  </div> <!-- .container -->
</nav>
